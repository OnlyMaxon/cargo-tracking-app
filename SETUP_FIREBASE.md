# 🔥 Инструкция по настройке Firebase для Next Cargo

## Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Добавить проект"** (Add project)
3. Введите название проекта (например, `next-cargo`)
4. Отключите Google Analytics (не обязательно для этого приложения)
5. Нажмите **"Создать проект"**

## Шаг 2: Настройка Web приложения

1. В консоли Firebase выберите ваш проект
2. Нажмите на значок **Web** (`</>`) для добавления веб-приложения
3. Введите название приложения (например, `Next Cargo Web`)
4. **НЕ** устанавливайте Firebase Hosting
5. Нажмите **"Зарегистрировать приложение"**

## Шаг 3: Получение конфигурации

После регистрации вы увидите объект `firebaseConfig`:

```javascript
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123..."
};
```

## Шаг 4: Создание файла .env

1. В корне проекта скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте файл `.env` и вставьте ваши данные из Firebase:

```env
VITE_FIREBASE_API_KEY=AIza...
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123...
```

## Шаг 5: Настройка Firestore Database

1. В консоли Firebase перейдите в **Firestore Database**
2. Нажмите **"Создать базу данных"** (Create database)
3. Выберите режим **"Тестовый режим"** (Test mode) для начала
4. Выберите регион (рекомендуется `europe-west1` для Европы)
5. Нажмите **"Включить"** (Enable)

## Шаг 6: Настройка правил безопасности Firestore

В разделе **Firestore Database → Rules** замените правила на следующие:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователи - только чтение
    match /users/{userId} {
      allow read: if true;
      allow write: if true;
    }
    
    // Пароли - только запись и чтение
    match /passwords/{userId} {
      allow read, write: if true;
    }
    
    // Заказы - все могут читать и писать
    match /orders/{orderId} {
      allow read, write: if true;
    }
    
    // Уведомления - все могут читать и писать
    match /notifications/{notificationId} {
      allow read, write: if true;
    }
    
    // Сессии - все могут читать и писать
    match /sessions/{sessionId} {
      allow read, write: if true;
    }
  }
}
```

> ⚠️ **ВАЖНО**: Эти правила открыты для разработки. Для production используйте более строгие правила с аутентификацией!

## Шаг 7: Запуск приложения

1. Убедитесь, что файл `.env` создан и заполнен
2. Запустите приложение:
   ```bash
   npm run dev
   ```
3. Откройте браузер на `http://localhost:5173`

## Шаг 8: Тестирование

При первом запуске автоматически создастся:
- **Админ аккаунт**: 
  - FIN: `ADMIN01`
  - Пароль: `admin123`
- **3 демо заказа** для тестирования

### Тестирование функций:

1. **Регистрация нового пользователя**
   - Перейдите на вкладку "Регистрация"
   - Введите имя, фамилию, FIN код (7 символов) и пароль
   - Нажмите "Зарегистрироваться"

2. **Вход в систему**
   - Используйте FIN код и пароль
   - Нажмите "Войти"

3. **Просмотр заказов**
   - После входа вы увидите список заказов
   - Нажмите на заказ для просмотра деталей

4. **Админ панель** (только для админов)
   - Войдите с учетной записью ADMIN01
   - Перейдите на вкладку "Админ"
   - Измените статусы заказов
   - Отправляйте уведомления

## Проверка данных в Firestore

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в **Firestore Database**
3. Вы должны увидеть коллекции:
   - `users` - пользователи
   - `passwords` - пароли
   - `orders` - заказы
   - `sessions` - сессии
   - `notifications` - уведомления (после отправки)

## Типичные проблемы и решения

### Ошибка: "Missing or insufficient permissions"
**Решение**: Проверьте правила безопасности Firestore (Шаг 6)

### Ошибка: "Firebase app not initialized"
**Решение**: Убедитесь, что файл `.env` существует и все переменные заполнены

### Данные не сохраняются
**Решение**: 
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что Firestore Database включен
3. Проверьте правила безопасности

### Не могу войти с ADMIN01
**Решение**: 
1. Очистите базу данных в Firestore
2. Перезапустите приложение для повторной инициализации

## Production правила безопасности (рекомендуется)

Для production используйте более строгие правила:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователи могут читать только свои данные
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Пароли не доступны для чтения
    match /passwords/{userId} {
      allow read: if false;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Заказы доступны только владельцу и админам
    match /orders/{orderId} {
      allow read: if request.auth != null && 
        (resource.data.userId == request.auth.uid || 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true);
      allow write: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }
    
    // Уведомления доступны только владельцу
    match /notifications/{notificationId} {
      allow read: if request.auth != null && resource.data.userId == request.auth.uid;
      allow write: if request.auth != null;
    }
    
    // Сессии доступны только авторизованным
    match /sessions/{sessionId} {
      allow read, write: if request.auth != null;
    }
  }
}
```

## Контакты

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Проверьте консоль Firebase на ошибки
3. Убедитесь что все шаги выполнены правильно

---

**Готово! 🎉** Ваше приложение Next Cargo теперь работает с Firebase!
