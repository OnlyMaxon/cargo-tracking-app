# 🔥 Инструкция по настройке Firebase для Next Cargo

## 📋 Что было сделано

✅ **AsyncStorage полностью удален** - теперь приложение работает только с Firebase  
✅ **Firebase Firestore настроен** для хранения данных  
✅ **Все коллекции готовы**: users, passwords, orders, notifications, sessions  
✅ **Демо данные автоматически создаются** при первом запуске  

## 🚀 Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Добавить проект"** (Add project)
3. Введите название проекта: `next-cargo` (или любое другое)
4. Отключите Google Analytics (не обязательно для этого проекта)
5. Нажмите **"Создать проект"**

## 🌐 Шаг 2: Настройка веб-приложения

1. На главной странице проекта нажмите значок **Web** (`</>`)
2. Введите название приложения: `Next Cargo Web`
3. **НЕ устанавливайте** Firebase Hosting (не нужен)
4. Нажмите **"Зарегистрировать приложение"**
5. Скопируйте конфигурацию Firebase (понадобится позже)

## 🗄️ Шаг 3: Создание базы данных Firestore

1. В левом меню выберите **"Firestore Database"**
2. Нажмите **"Создать базу данных"** (Create database)
3. Выберите режим: **"Начать в тестовом режиме"** (Start in test mode)
   - Это позволит начать работу без сложных правил безопасности
4. Выберите регион: **eur3 (europe-west)** или ближайший к вам
5. Нажмите **"Готово"** (Done)

## 🔐 Шаг 4: Настройка правил безопасности

После создания базы данных настройте правила безопасности:

1. Перейдите на вкладку **"Правила"** (Rules)
2. Замените правила на следующие:

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешить всем читать и писать (для разработки)
    match /{document=**} {
      allow read, write: if true;
    }
    
    // ⚠️ ВНИМАНИЕ: Это правила для РАЗРАБОТКИ
    // Для продакшена нужны более строгие правила!
  }
}
```

3. Нажмите **"Опубликовать"** (Publish)

### 🛡️ Продакшен правила (для финальной версии)

Когда будете готовы к продакшену, используйте эти правила:

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователи: читать может любой, писать только сам пользователь
    match /users/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Пароли: только чтение для авторизации
    match /passwords/{userId} {
      allow read: if true;
      allow write: if false;  // Только через функции или админ SDK
    }
    
    // Заказы: читать свои заказы, админы могут всё
    match /orders/{orderId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null;
    }
    
    // Уведомления: читать только свои
    match /notifications/{notificationId} {
      allow read: if request.auth != null && 
                     resource.data.userId == request.auth.uid;
      allow write: if request.auth != null;
    }
    
    // Сессии: только для авторизованных
    match /sessions/{sessionId} {
      allow read, write: if true;
    }
  }
}
```

## 🔑 Шаг 5: Получение API ключей

1. В левом меню нажмите на значок **⚙️ (Настройки)** > **"Настройки проекта"**
2. Прокрутите вниз до раздела **"Ваши приложения"**
3. Найдите свое веб-приложение
4. В разделе **"Конфигурация SDK"** скопируйте значения:
   - `apiKey`
   - `authDomain`
   - `projectId`

## 📝 Шаг 6: Создание файла .env

1. В корне проекта создайте файл `.env` (точная копия `.env.example`)
2. Вставьте ваши ключи из Firebase:

```env
VITE_FIREBASE_API_KEY=AIzaSyC_ваш_ключ_здесь
VITE_FIREBASE_AUTH_DOMAIN=next-cargo-12345.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=next-cargo-12345
```

### ⚠️ Важно:
- Файл `.env` **НЕ должен** попадать в Git (уже добавлен в `.gitignore`)
- **НЕ делитесь** этими ключами публично
- Для каждого окружения (dev/prod) используйте отдельные проекты Firebase

## ✅ Шаг 7: Проверка работы

1. Сохраните файл `.env`
2. Перезапустите сервер разработки:
   ```bash
   npm run dev
   ```
3. Откройте приложение в браузере
4. При первом запуске автоматически создастся:
   - **Админ аккаунт**: 
     - FIN: `ADMIN01`
     - Пароль: `admin123`
   - **3 демо заказа** для админа

5. Войдите с админ данными и проверьте работу

## 🔍 Проверка данных в Firebase

1. Откройте **Firebase Console** > **Firestore Database**
2. Вы должны увидеть коллекции:
   - `users` (1 документ - админ)
   - `passwords` (1 документ)
   - `orders` (3 демо заказа)
   - `sessions` (после входа)

## 🎯 Структура данных в Firestore

### Коллекция `users`
```
users/
  {userId}/
    - id: string
    - firstName: string
    - lastName: string
    - finCode: string
    - isAdmin: boolean
```

### Коллекция `passwords`
```
passwords/
  {userId}/
    - password: string
```

### Коллекция `orders`
```
orders/
  {orderId}/
    - id: string
    - userId: string
    - trackingNumber: string
    - status: 'in-transit' | 'warehouse' | 'delivered'
    - title: string
    - description: string
    - weight: string
    - from: string
    - to: string
    - createdAt: number
    - updatedAt: number
    - statusHistory: array
```

### Коллекция `notifications`
```
notifications/
  {notificationId}/
    - id: string
    - userId: string
    - orderId: string
    - message: string
    - read: boolean
    - createdAt: number
```

### Коллекция `sessions`
```
sessions/
  current/
    - userId: string
```

## 🚨 Решение проблем

### Проблема: "Firebase: Error (auth/api-key-not-valid)"
**Решение**: Проверьте правильность `VITE_FIREBASE_API_KEY` в файле `.env`

### Проблема: "Missing or insufficient permissions"
**Решение**: Проверьте правила безопасности в Firestore (Шаг 4)

### Проблема: Данные не сохраняются
**Решение**: 
1. Проверьте консоль браузера на ошибки
2. Убедитесь что Firestore создан в тестовом режиме
3. Проверьте правила безопасности

### Проблема: Приложение не запускается
**Решение**:
1. Убедитесь что файл `.env` в корне проекта
2. Перезапустите сервер: `npm run dev`
3. Очистите кеш браузера

## 📦 Используемые Firebase пакеты

Все необходимые пакеты уже установлены:
- `firebase` - основной SDK
- `firebase/firestore` - база данных
- `firebase/auth` - аутентификация (подготовлено для будущего)

## 🎓 Полезные ссылки

- [Firebase Console](https://console.firebase.google.com/)
- [Документация Firestore](https://firebase.google.com/docs/firestore)
- [Правила безопасности](https://firebase.google.com/docs/firestore/security/get-started)

## ✨ Готово!

Теперь ваше приложение **Next Cargo** полностью работает с Firebase! 🎉

Все данные хранятся в облаке, и вы можете:
- ✅ Регистрировать пользователей
- ✅ Создавать и отслеживать заказы
- ✅ Управлять статусами (админ панель)
- ✅ Отправлять уведомления
- ✅ Сохранять сессии пользователей

---

**Создано для Next Cargo** 📦
Версия: 1.0.0
