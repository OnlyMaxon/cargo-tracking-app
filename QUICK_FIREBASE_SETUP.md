# 🔥 Быстрая настройка Firebase для Next Cargo

## ✅ Что уже готово

Firebase полностью интегрирован в приложение! AsyncStorage удален. Осталось только добавить ваши API ключи.

## 📋 Шаги настройки (5-10 минут)

### 1️⃣ Создайте Firebase проект

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Добавить проект"**
3. Название: `next-cargo` (или любое другое)
4. Отключите Google Analytics (не нужен)
5. Нажмите **"Создать проект"**

### 2️⃣ Добавьте веб-приложение

1. На странице проекта нажмите иконку **Web** (`</>`)
2. Название: `Next Cargo Web`
3. **НЕ включайте** Firebase Hosting
4. Нажмите **"Зарегистрировать"**
5. **Скопируйте конфигурацию** (понадобится в шаге 5)

### 3️⃣ Создайте Firestore базу данных

1. В меню слева: **"Firestore Database"**
2. **"Создать базу данных"**
3. Режим: **"Тестовый режим"** (test mode)
4. Регион: **eur3 (europe-west)** или ближайший
5. **"Готово"**

### 4️⃣ Настройте правила безопасности

1. Вкладка **"Правила"** (Rules)
2. Замените на этот код:

```javascript
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

3. Нажмите **"Опубликовать"**

⚠️ **Важно**: Эти правила для разработки. Для продакшена используйте более строгие правила (см. FIREBASE_SETUP_INSTRUCTIONS.md)

### 5️⃣ Добавьте ключи в проект

1. Создайте файл `.env` в корне проекта
2. Скопируйте и вставьте:

```env
VITE_FIREBASE_API_KEY=ваш_api_key
VITE_FIREBASE_AUTH_DOMAIN=ваш_проект.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=ваш_project_id
```

3. Замените значения на ваши из Firebase Console:
   - **Настройки проекта** (⚙️) > **Настройки проекта**
   - Секция **"Ваши приложения"** > **SDK setup and configuration**
   - Скопируйте: `apiKey`, `authDomain`, `projectId`

### 6️⃣ Запустите приложение

```bash
npm run dev
```

## 🎯 Проверка работы

1. Откройте приложение в браузере
2. Войдите с демо данными:
   - **FIN**: `ADMIN01`
   - **Пароль**: `admin123`
3. Откройте Firebase Console > Firestore
4. Проверьте что созданы коллекции:
   - `users`
   - `passwords`
   - `orders`
   - `sessions`

## ✅ Готово!

Приложение работает с Firebase! Теперь все данные в облаке.

## 📖 Подробная документация

- Полная инструкция: `FIREBASE_SETUP_INSTRUCTIONS.md`
- Продакшен правила безопасности
- Структура данных
- Решение проблем

## 🔒 Безопасность

- ❌ **НЕ** публикуйте файл `.env` в Git
- ✅ `.env` уже добавлен в `.gitignore`
- ✅ Используйте отдельные проекты для dev/prod

---

**Next Cargo** v1.0.0 | Firebase Edition
